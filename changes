import axios from 'axios';

// Axios instance
const api = axios.create({
  baseURL: 'https://10.191.171.12:5443/PyPortal/',
  timeout: 100000,
  headers: {
    'Content-Type': 'application/json',
  },
});

// 🔸 Service-wise POST
export const postServiceWiseExp5 = async (time) => {
  try {
    const response = await api.post('EISHome/servicewise_top5_FiveM/EXP/', { time });
    return response;
  } catch (error) {
    console.error('Error fetching Service-wise EXP data:', error);
    throw error;
  }
};

export const postServiceWiseSys5 = async (time) => {
  try {
    const response = await api.post('EISHome/servicewise_top5_FiveM/SYS/', { time });
    return response;
  } catch (error) {
    console.error('Error fetching Service-wise SYS data:', error);
    throw error;
  }
};

// 🔸 IP-wise POST
export const postIpWiseExp5 = async (time) => {
  try {
    const response = await api.post('EISHome/ipwise_top5_FiveM/EXP/', { time });
    return response;
  } catch (error) {
    console.error('Error fetching IP-wise EXP data:', error);
    throw error;
  }
};

export const postIpWiseSys5 = async (time) => {
  try {
    const response = await api.post('EISHome/ipwise_top5_FiveM/SYS/', { time });
    return response;
  } catch (error) {
    console.error('Error fetching IP-wise SYS data:', error);
    throw error;
  }
};

// 🔸 Port-wise POST
export const postPortWiseExp5 = async (time) => {
  try {
    const response = await api.post('EISHome/portwise_top5_FiveM/EXP/', { time });
    return response;
  } catch (error) {
    console.error('Error fetching Port-wise EXP data:', error);
    throw error;
  }
};

export const postPortWiseSys5 = async (time) => {
  try {
    const response = await api.post('EISHome/portwise_top5_FiveM/SYS/', { time });
    return response;
  } catch (error) {
    console.error('Error fetching Port-wise SYS data:', error);
    throw error;
  }
};
