.footer-custom {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 10px 20px;
  background-color: #333;
  color: white;
  font-family: Arial, sans-serif;
  height: 50px;
  overflow: hidden;
}

.footer-left {
  position: relative;
  z-index: 3;
  font-weight: bold;
  font-size: 14px;
}

.footer-right {
  position: relative;
  z-index: 3;
  font-weight: bold;
  font-size: 14px;
}

.footer-announcement-marquee {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  display: flex;
  align-items: center;
  z-index: 1;
  overflow: hidden;
}

.footer-announcement-marquee marquee {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
}

.footer-announcement {
  font-weight: bold;
  color: white;
  font-size: 14px;
  white-space: nowrap;
}

/* Alternative CSS animation approach for better browser compatibility */
@keyframes scroll-left {
  0% {
    transform: translateX(100%);
  }
  100% {
    transform: translateX(-100%);
  }
}

.footer-announcement-scroll {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  display: flex;
  align-items: center;
  z-index: 1;
  overflow: hidden;
}

.footer-announcement-text {
  animation: scroll-left 15s linear infinite;
  font-weight: bold;
  color: white;
  font-size: 14px;
  white-space: nowrap;
}
